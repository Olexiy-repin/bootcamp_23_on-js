'use strict';
/*
 * Події клавіатури. keypress, keydown, keyup
 */

// const outputEl = document.querySelector('.js-output');

// window.addEventListener('keydown', event => {
//   outputEl.insertAdjacentHTML('afterbegin', `code: ${event.code} key: ${event.key} <br> <hr>`);
// });
